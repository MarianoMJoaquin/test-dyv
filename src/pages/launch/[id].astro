---
import { getLaunchBy } from '../../services/spacex'

const { id } = Astro.params

let launch
if (id){
    launch = await getLaunchBy({ id })
}

console.log(launch)
---


<article>
    {launch.id}
</article>